#EEG Eye State Dataset URL
http://archive.ics.uci.edu/ml/datasets/EEG+Eye+State

#Github Repo for Code, Data
https://github.com/tracetidwell/cs7641/tree/master/hw_2

There are three main files for code: eye_data.ipynb, bank_data.ipynb, utils.py

eye_data.ipynb
Sections:
	k-Means
	Expectation Maximization
	PCA
	k-Means on PCA
	EM on PCA
	NN on PCA
	NN on k-Means, PCA
	NN on EM, PCA
	NN on k-Means + PCA I (adding clusters as features after dimensionality reduction)
	NN on k-Means + PCA II (adding clusters as features before dimensionality reduction)
	ICA
	k-Means on ICA
	EM on ICA
	NN on ICA
	NN on k-Means, ICA
	NN on EM, ICA
	NN on k-Means + ICA
	Randomized Projection
	t-SNE
	NN on t-SNE

eye_data.ipynb
Sections:
	k-Means
	Expectation Maximization
	PCA
	k-Means on PCA
	EM on PCA
	ICA
	k-Means on ICA
	EM on ICA
	Randomized Projection
	t-SNE
	NN on t-SNE

utils.py
	Primarily methods for loading and plotting data

Python Environment
	python 3.7
	scikitlearn
	pandas
	joblib
	numpy
	matplotlib

Running the Jupyter notebooks is very straightforward. All of the necessary libraries are imported at the top, and any additional methods are also defined. 